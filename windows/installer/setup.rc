// The installer's own icon. The payload is a second .rc generated by CMake,
// because the paths it needs are build outputs and only the build knows them.

#include "installer/setup_resource.h"

IDI_XCAM_SETUP ICON "../app/xcam.ico"
